public boolean isInteger(String str)
{
    int len = str.length();
    if (len == 0) {
        return false;
    }

    for (int i = 0; i < len; ++i) {
        if (!Character.isDigit(str.charAt(i))) {
            return false;
        }
    }
    return true;
}
